# RDW batch — what the adversaries found

Verbatim return values from the five verify agents of run `wf_2a267b11-e28`,
2026-09-05. Four of five REFUTED. The suites were green when they did it —
window 134, keys 71, store 130, control 485 — which is this batch's oldest
lesson arriving again: **a suite fences the questions its author thought of.**

Reading order by harm: **#5 (R3)** puts wrong text on the clipboard and wipes
it; **#4 (R5)** states a false fact about the circuit; **#2 (R2)** did not
deliver half its own subject line; **#3 (R4)** works on neither of the servers
the user actually uses (issue 1343).

VERIFY #1 = R1, #2 = R2, #3 = R4, #4 = R5, #5 = R3 — the batch's run order.


########## VERIFY #1 :: HOLDS_WITH_CAVEAT

 * I COULD NOT REFUTE R1. The mechanism is right, the fences are non-vacuous under six sabotages, and the one pixel claim that mattered - 'the ENTIRE line' - is TRUE by direct XGetImage measurement (#d7d7d7 across the full 779px pane width, including at width-3, against #ffffff above and below). The implementer's numbers all reproduce.

 * CAVEAT 1, THE ONE THAT MATTERS: this item's deliverable is a shade of grey and NOBODY HAS LOOKED. Look debt the_RDW_line_cursor_shade is open and unpaid. Suites green, please look - R1 is not done until the user says #d7d7d7 on #ffffff is 'noticeably' darker. I can say the step is 40 of 255 and uniform across the line; I cannot say it is noticeable.

 * CAVEAT 2, NEW, MEASURED, UNFENCED: on a CURSORED line an INACTIVE selection is three times harder to see. #c3c3c3 (Tk's inactiveselectbackground) on the #d7d7d7 cursor shade is 20 of 255; the same selection on an uncursored line is #c3c3c3 on #ffffff, 60 of 255. Row CU10 is the fence for 'the selection still shows over the cursor' and its fixture does `focus -force .rdw.p.t` first, so it measures ONLY the focused case (#4a6984, obvious). The unfocused case is the one the user meets, because the RDW's own focus rule (issue 1308 / DD-12) puts the keyboard back on the canvas the moment they click the schematic. Nothing is broken - the tag order is correct and both colours are Tk defaults R1 did not choose - but it lands on item R3's subject. Look debt filed.

 * CAVEAT 3: NO :0 RUN EXISTS FOR THIS CHANGE. CLAUDE.md's rule is 'run a GUI feature's suite on :0 once before calling it done', and R1 is a GUI feature end to end. The implementer ran :99 only; I deliberately did NOT discharge it unattended - a :0 run maps windows onto the user's WSLg desktop while they are away, and the GUI gate exists to stop exactly that. The standing suite debts test_rdw_keys_1245 and test_rdw_window_1245 already cover it and remain open. The driver should pay them with owed.sh drain when the user is back, not silently.

 * FINDING FOR THE DRIVER, BEFORE R2 LANDS: shift-click moves the line cursor. Measured - click line 4, Shift-click line 11: sel becomes 4.0-11.5 AND the cursor moves to 11, because the widget-level <Button-1> binding matches an event carrying extra modifiers and no <Shift-Button-1> is bound above it. So the standard click-then-shift-click multi-line select gesture silently re-aims Delete/Add/Up/Down at the LAST line of the selection. It is defensible today (the shade is visible, so nothing is hidden) and no row fences it. R2 makes Up/Down act on that row, so R2's crew should decide deliberately whether a shift-click is a cursor move or a selection-only gesture.

 * TEST-QUALITY NOTE, not a defect: CU7's last leg asserts `[focus]` eq `.rdw.p.t`, but cu_fixture does `focus -force .rdw.p.t` immediately before the click, so that leg cannot fail whatever the click does to focus. It is vacuous. It is also not the row's subject, and the row's real legs (span, count, -state, buffer bytes) are all sharp - the `lineend` sabotage reds CU7 correctly.

 * CLAUDE.md CORRECTION THE DRIVER SHOULD KNOW ABOUT, because DD-8 depends on it: the three-server table says `:0` answers vendor 'Microsoft Corporation'. Measured today with xdpyinfo, `:0` answers 'The X.Org Foundation', X.Org version 24.1.6 - the same vendor string Xvfb gives. The distinction the table is load-bearing for still holds ($DISPLAY is 172.20.160.1:0, the TCP Windows server; :99 is our Xvfb; :0 is a third, unix-socket-only server), but a crew told to identify the displays BY VENDOR STRING will now mis-sort :0 and :99. DD-8 sends item R3 to the user's real X server on the strength of that table.

 * NOT FIXED, and correctly said loudly by the implementer: issue 1330 (rdw::_apply_now swallows an apply failure while the status line reports success) and issue 1331 are untouched. I confirmed R1 adds no caller of that channel. R2 is where it becomes load-bearing.

 * THE DARK HALF OF DD-2 IS UNVERIFIABLE IN THIS TREE, and the implementer disclosed it. Confirmed at src/ase_window.tcl:165: ase::palette is one hard-coded light dict with no dark variant, so #202020 -> #484848 is measured through a test stub only. The derivation itself is sound - the literal-shade sabotage reds CU3 and CU4 - but no real dark pane exists to check it against.

 * THE OVERTURNED COMMENT (rdw::_target_line no longer reads the pane's insert mark) IS SAFE. I verified independently that no code anywhere in src/ reads `.rdw.p.t index insert`; the four `index insert` hits in the tree are in xschem.tcl and ciw.tcl on unrelated widgets. All 109 pre-existing window rows and all 41 pre-existing keys rows survive it, by name.

########## VERIFY #2 :: REFUTED

 * **REFUTATION — the SUMMARY list (key 2) half of the item's own subject line is not delivered, and R2 newly makes the window CONTRADICT the sheet.** Driven headless and again on :99 with a live pane (`/tmp/claude-1000/-home-analog-dev-xschem-claude/1e23e38a-228a-491e-a9b0-387bda9d283e/scratchpad/probe2.tcl`, `probe3.tcl`): with `rdw::set_list summary`, cursor on the `gds` row, two `rdw::button up` presses give `store summary: ids gm gds -> gds ids gm`, `PANE: {ids gds gm} -> {gds ids gm}` (moved=1), and `op_annot::text M1` **byte-identical before and after**: `id  =\ngm  =\ngds =\n` (moved=0). `shown` is `id gm gds` throughout. So after a key-2 reorder the window says `gds` first and the schematic still draws `id` first, the status line says the plain `Up: moved gds up in the summary list for class ad2cls.` with no caveat, and the disagreement survives switching back to list 1. **It is structural, not a bug in R2's code**: `op_param_lists::_show_set` (src/op_param_lists.tcl:1746) builds `shown` by filtering the union with `effective $cls **annotation**`, and `op_annot::text` (src/op_annot.tcl:2046) draws `shown` in `shown`'s order — the summary list's order can never reach the sheet. PLAN.md R2 half 2 and DD-4 both name list 2 explicitly; the LEDGER row and doc/claude/issues/1338-*.md both assert DD-4 was "honoured as written, neither found wrong" and that "DD-4's 'lists 1 and 2 re-render' already held". That is false for list 2. **Before R2 the two agreed** (both stood still — the implementer's own measurement, `::rdw::blocks` byte-identical); R2 moves one of them.

 * **The fence that was supposed to catch this measures the wrong thing.** Row RE7 golds `annot_overlay_flushes` `>= 1` for an accepted summary reorder. I measured that delta as **+2** for the two presses above while the drawn text was byte-identical — `op_annot::register` bumps `::op_annot::gen` on a re-register even when `params`/`shown` come back the same. So RE7 passes on a counter, not on the sheet's content. This is exactly CREW_BRIEF's "a suite fences the questions its author thought of": the row would go green with the summary order permanently invisible on the sheet, which is the shipped state.

 * **RE5's own stated invariant is false the moment a new dump lands.** `rdw::_reorder_shown`'s comment says a window that reordered only the cursored block "would show the SAME class list in two different orders at once"; RE5's title says "the window never shows one class list in two different orders at once". Driven (probe2 FINDING 2, and on the live pane in probe3 section D): after two Ups the M1 block reads `gds ids gm`; one further `rdw::push` of the same device — the door `rdw::key 1` -> `rdw::show` -> `rdw::dump` -> `rdw::dump_devpath` uses, and `rdw::push` (src/rdw.tcl:915) does **not** re-slot — lands a block reading `ids gds gm` (raw order) above it. Live pane after the re-dump: `ids gds gm | ids gm | gds ids gm`. The OLDEST dumps carry the NEWEST order and the NEWEST dump carries the raw one. Not fenced by any row; the E-question rule debt covers only "an older dump re-orders under you", not "and the newest one then disagrees with it".

 * **A narrow (flavor) reorder reshuffles a block the edit explicitly did not touch.** probe2 FINDING 3: with a flavor entry `set_list flavor {ad2cls <cellM1>} annotation ...` governing M1's cell, an Up on M1's `gds` writes only that entry — the status line says so verbatim: `Up: moved gds up in the annotation list for cells matching .../ad2n.sym of class ad2cls.` — and the CLASS annotation list is unmoved (`ids gm gds` before and after). But M2's block, a different cell governed by the unmoved class entry, still goes `gm ids -> ids gm`. `rdw::_reorder_shown` re-slots every block whose subject resolves to the class, so a press the sentence says was cell-scoped visibly reorders another cell's block. RE5's fixture has no flavor entry, so no row sees this.

 * **Lesser: Delete/Add leave the pane and the store disagreeing, and the stated reason for that does not support the choice.** probe5: after an Up (pane == store, `ids gds gm`), a broad Delete of `gds` from list 1 then an Add of it from list 3 leaves `store: ids gm gds` against `pane: ids gds gm`, with no re-slot and no sentence. `rdw::button`'s comment justifies not re-slotting on that arm because a re-slot "could neither add the new row (no run published it) nor remove the deleted one (the run still did)" — that is an argument about MEMBERSHIP; re-slotting only the rows the run published AND the list declares would keep the ORDER in agreement while adding and removing nothing. So the property R2 just taught the user to rely on is maintained by Up/Down alone.

 * **What I could NOT break, and drove:** the multi-primitive re-slot never crosses a `  <rawdev>` sub-header; an undeclared row keeps its slot; the cursor follows the row across ten consecutive presses and refuses by name at the top; a closed-then-reopened window repaints the cursor on the moved row (`cursortag 10.0 11.0`, `targetrow 10`); `.rdw` is a singleton so "two windows" is unreachable; an empty store, a cursor at line 999, a cursor on a header line, and a class block with no parameter rows at all each produce the right refusal and change nothing; `_reslot_block` is a strict permutation so its "disagree" arm is unreachable; `rdw::_list_params`'s dedupe and per-cell `effective` are correct; and the issue-1330 fix is real (`_apply_now` answers rather than swallowing).

 * **Not my call to fix (per the brief), but the cheap shapes are:** for the refutation, either say it — a sentence on the key-2 arm plus an issue filed against `_show_set`/`op_annot::text` — or stop re-slotting the pane by the summary list; and tighten RE7's summary leg to gold `op_annot::text`'s STRING, not the flush counter. For the RE5 invariant, re-slot in `rdw::push` (or in `render_pane`) rather than only in `rdw::button`.

########## VERIFY #3 :: REFUTED

 * REFUTATION 1 - THE FIX NAMED IN THE COMMIT DOES NOT FIRE. The commit message, the issue file and the look debt all say the re-map's keyboard grab is 'Fixed by arming the EXISTING one-shot hand-back (rdw::_arm_focus_handback ... a new `remapping` argument tells it a map really is coming)'. Measured: that hand-back NEVER FIRES once the pane has been clicked even once - the single gesture this window exists for (issue 1306, quoted at src/rdw.tcl:1621: 'the one focus this window is entitled to keep and the whole reason the window exists'). Tk then remembers .rdw.p.t as .rdw's focus child, so the window manager's map-time grant is routed to the CHILD, and rdw::_focus_handback's discriminator - `if {$land ne {.rdw}} { return 0 }`, src/rdw.tcl:1686 - rejects it. TRACE, :99/openbox, HEAD, one bare rdw::push: '-> rdw::_raise' 1430.3 ms / 'Unmap .rdw' 1430.6 / 'Map .rdw' 1435.3 / '<- rdw::_raise' 1435.6 / '-> _focus_handback (pending=1)' 1436.4 / 'FocusIn .rdw detail=NotifyVirtual focus=.rdw.p.t' - _focus_handback entered THREE times, pending=1, declining each time; the keyboard ends on .rdw.p.t and is still there 1.2 s later. The same probe against a HEAD~1 worktree on the same binary: keyboard .drw, pending 0. Deterministic 5/5.

 * REFUTATION 2 - ::rdw::focus_pending IS LEFT ARMED FOREVER, on the SHIPPED key path too. Measured 5/5 through rdw::show (the proc a bare `1` runs) and 3/3 on every variant in probe 6: pending=1 after the dump and it never clears. That is exactly the case the narrowing this item widened was written against - src/rdw.tcl:1616, 'an armed flag left lying around is a bounce waiting to happen' - and it DOES bounce: land the keyboard on .rdw itself afterwards (a click on the window frame or on the button column; Tk buttons take no focus on X, so [focus] stays .rdw) and the one-shot fires, yanking the keyboard to the schematic. Driven and observed: 'after landing the keyboard on .rdw itself: focus=.drw pending=0'. Pre-R4, pending is 0 throughout the same sequence.

 * REFUTATION 3 - WHAT ACTUALLY KEEPS THE KEYBOARD ON THE CANVAS IS A ~0.4-0.8 ms RACE THIS FILE SAYS CANNOT BE WON. With the hand-back inert, the only surviving mechanism on the key/pick path is rdw::show's synchronous rdw::_focus_canvas (`focus -force`). Traced: `_raise` returns at 1440.2 ms and `_focus_canvas` runs at 1440.3; on the identical path with no `_focus_canvas` the WM grant lands at +0.8 ms. So the client wins by well under a millisecond. src/rdw.tcl:1599-1609 records the opposite outcome for the first-open case in this very file - 'THE HAND-BACK IS EVENT DRIVEN, BECAUSE A SYNCHRONOUS ONE CANNOT WIN THE RACE ... ONE `update` later it is on .rdw and it stays there'. Stub `_focus_canvas` out and the keyboard is in the window 3/3. A sub-millisecond ordering measured on a local Xvfb is not evidence for the user's VcXsrv server reached over TCP - and that server is the entire reason this item exists, since :99's plain `raise` is green here and inert there.

 * REFUTATION 4 - THE SUITE IS BLIND TO ALL OF IT, AND ITS OWN ROW WOULD FAIL. Section RA's ra_fixture/ra_park open the window, park a decoy over it and force focus to .drw; nothing anywhere in section RA ever clicks the pane. I rebuilt RA1's fixture exactly and added that ONE click: RA1's focus leg then reads `.rdw.p.t` where the row expects `.drw` (its other legs - above 0->1, mapped 1 - still pass), and pending is 1. The same fixture against the pre-R4 tree reads `.drw`. So the row meant to fence 'the keyboard still on the canvas because the user said no need to focus' passes only in the never-yet-used state. This is the batch's own recurring failure mode: a suite fences the questions its author thought of.

 * SCOPE OBSERVATION (not a defect, but unratified and unnamed anywhere): a dump that reports NOTHING still yanks the window to the front. Pushed a block with zero device rows against a parked decoy: above 0 -> 1, Map=1 Unmap=1. Combined with the pick mode's block-per-click, the window now comes forward on every click including the ones that have nothing to say.

 * NOT A DEFECT OF THIS ITEM, checked and cleared rather than charged: a dump destroys the pane's `sel` range and the PRIMARY selection (sel {1.0 1.5} -> {}, PRIMARY 5 chars -> 0). Measured identically on the pre-R4 tree, and reproduced by calling rdw::render_pane alone with no push at all. Pre-existing. It matters for item R3 and should be carried there.

 * INCONCLUSIVE, REPORTED SO NOBODY MISREADS IT LATER: on Xwayland (:0) the shipped key path leaves the keyboard on .rdw.p.t at HEAD - but the pre-R4 tree does the same there, and even a bare `focus -force .drw` fails to stick (row C flapped in 2 of 3 runs). :0 therefore does not isolate R4. What it does show is that the `focus -force` this change now depends on is not reliable on every server.

 * WHAT SURVIVED THE ATTACK, said plainly: THE RAISE ITSELF IS SOUND. The stacking really moves (decoy above 0 -> 1), the re-map receipts are real (Map/Unmap counted on .rdw itself, `_remap_verify` really scheduled and run), a closed window is not conjured, an iconified one comes back mapped and normal, ten consecutive dumps leave `wm geometry` byte-identical, the split is structurally correct with exactly one definition of each half and no name collision, `raise_activate_toplevel` keeps its two guards and its `xschem activate_window` for the fourteen other callers, test_remap_verify is green and T1 is at zero. The refutation is confined to the FOCUS half - but that half is the one the user forbade in the same sentence as the request, and it is the half the commit claims to have fixed. The driver decides revert or repair; the obvious repair is that the hand-back's discriminator must accept a grant that lands on a DESCENDANT of .rdw when it was armed with remapping=1, while still rejecting a deliberate click (the two are distinguishable by the armed flag plus the FocusIn detail - NotifyVirtual/NotifyNonlinearVirtual on the toplevel with [focus] on the child - not by the name alone), and the flag must not be left armed when no grant arrives.

########## VERIFY #4 :: REFUTED

 * REFUTATION (CONFIRMED, driven in a live pane on :99). `rdw::_value_text` decides "this value is non-finite" by seeing an EMPTY string come back from `op_annot::eng_or_blank`. That proc returns empty for TWO different reasons, and the code cannot tell them apart: (a) the value really is nan/inf, and (b) `to_eng` could not format a perfectly finite number, because `eng_or_blank` CATCHES to_eng's raise internally and answers `{}`. Case (b) is reachable from a shipped menu. `Simulation > Set netlist / graph / annotation precision` (src/xschem.tcl:17738-17740) is a bare `input_line` free-text entry with NO validation; its OK button runs `eval set ev_precision [.dialog.f1.e get]`, so anything that is not a plain non-negative integer STICKS. Measured on this binary, all of these stick and all of them break the format: `-1`, `2.5`, `abc`, `4x`, `+4`, `0x4`, `6.`, `6.0`. From that moment `format %.${pr}g` raises inside `to_eng` (src/xschem.tcl:1928/1930), eng_or_blank answers `{}`, and EVERY finite, correctly measured value in the Results Display Window prints `(did not converge)`. HOW I DROVE IT, end to end in the real widget on :99: `rdw::open`; push a block at the shipped precision 4; `eval set ::ev_precision -1` (exactly what the menu does with a typed `-1`); push the SAME answer dict again. The pane holds both, and reads:
    M1:/xdut/xbg / @m.x1.m1 / id : (did not converge) / gm : (did not converge) / vth : (did not converge)
    M1:/xdut/xbg / @m.x1.m1 / id : 11.1u / gm : 1m / vth : 0.75
Same device, same numbers, same raw, one session. Stored blocks keep their rendered text, so the window shows a MIXTURE and it reads exactly like a circuit that stopped converging partway through a session. WHY THIS IS THIS ITEM'S DEFECT AND NOT INHERITED: before R5 the proc body was `return [rdw::_oneline $v]` (read from the commit diff, not run), so the window printed the true value whatever ev_precision was. AND IT BREAKS R5'S OWN HEADLINE PROMISE: in that state the SHEET blanks the row (`op_annot.tcl:2122-2128` emits `id =` when eng_or_blank returns empty) while the WINDOW asserts non-convergence, so the two surfaces DO disagree - the one thing DD-7 and this item exist to stop. The commit message and doc/claude/issues/1341-*.md both state the exact principle being violated ("Answering `(did not converge)` there would invent a non-convergence for a number the simulator computed perfectly well, which is the plausible-wrong-number failure invariant I3 exists to prevent") and build the NOFMT sentinel for it - but NOFMT only survives when the CALL RAISES, i.e. only when `op_annot` never loaded, which the issue file itself calls unreachable-as-shipped. The reachable half is uncovered. NO SUITE ROW SEES IT: EN6 is the only row that touches ev_precision and it drives 4 and 6 only. NOT FIXED, per instruction - the discriminator is already in the tree and already called by eng_or_blank itself (`op_annot::_finite`, op_annot.tcl:1178-1182; measured: 0 for nan/-nan/NaN/inf/-inf/Inf/INF/Infinity/1e400/1e309, 1 for every finite double), so a repair need not add a second spelling of "is this finite". Driver decides revert or repair.

 * LATENT, not reachable through the shipped producer, but the file's own comment overstates the gate. `_value_text` sends every string that passes `string is double -strict` into `to_eng`, which is `uplevel #0 expr [join $args]` - so `expr` REBASES numeric literals. Measured: `010` -> `8`, `007` -> `7`, `00000000012` -> `10`, `0x10` -> `16`, `0b101` -> `5`. A leading-zero value silently prints as a different number. It is latent today because `ase::backend::ngspice::op_param_set` (src/ase.tcl:9044) fills the `devices` bucket only from `op_annot::raw_class` -> `xschem raw value`, i.e. C-formatted decimals, and `ase::register_backend` has exactly one registrant. The plausible way in is a future text-parsing producer such as the blanket `set altshow` dump (issues 1333-1336, landed at a5e15dda). Two things worth the driver's eye: src/rdw.tcl:455-459 says the double gate is "a SECOND lock ... so a value that reached it unguarded would EVALUATE at global scope" - true only for NON-numeric strings; every numeric string still reaches `expr` at global scope. And row EN7 pins `0x10 -> 16` as ACCEPTABLE (by equality with the sheet), which fences the rebasing in place rather than flagging it.

 * DEAD ARM, and it changes how the open rule debt should be read. On the shipped seam a non-finite NEVER lands in the `devices` bucket: `op_annot::raw_class` (op_annot.tcl:1246-1252) routes it to the `nonfinite` bucket, which `format_answer` renders through `_nonfinite_text` directly at src/rdw.tcl:626-627, not through `_value_text`. So the non-finite arm of `_value_text` that row EN4 fences has no live producer - and the ONE reachable way to make that arm fire on this tree is finding #1, a finite number the formatter could not format. The driver should know this when weighing rule debt `1341_nonfinite_in_the_devices_bucket`: the option being ratified is currently exercised only by the bug.

 * MINOR TEST FRAGILITY, introduced by this item. Row EN6 asserts `$EN6_SAVE` equals the literal `4` (tests/headless/test_rdw_window_1245.tcl:5022, expectation list `[list 4 12.35u 12.3457u 12.35u]`), so the suite now hard-depends on the user's `ev_precision` sitting at its shipped default - the very preference this item advertises support for. `set_ne ev_precision 4` (src/xschem.tcl:18540) is only a default; a `~/.xschem/xschemrc` carrying `set ev_precision 6` reds EN6 (and only EN6 - I checked the other goldens: 1.11e-05 is `11.1u` at both 4 and 6, 1.2e-05 is `12u` at both) with nothing whatever wrong in the tree. Reading the saved value instead of asserting it would keep the row's real subject, which is that 4 and 6 differ.

 * EVERYTHING ELSE THE IMPLEMENTER CLAIMED CHECKS OUT, verified rather than taken on trust. (a) All six suite numbers reproduce exactly, each with a real RESULT line. (b) T1 is at ZERO run solo. (c) The golden re-measurements in both test files are numerically correct and none is weakened - I diffed every removed literal against its replacement (1.11e-05->11.1u, 0.001->1m, 1e-06->1u, 1e-15->1f, 4e-06->4u, 5.6e-06->5.6u, 1.2e-05->12u, and `0`/`0.75`/`1.25`/`0.5` unchanged); no exact assertion became a loose one. (d) `src/rdw.tcl` names `to_eng` ZERO times outside comments (grep: line 486 is the only call, to `::op_annot::eng_or_blank`). (e) The value column width really is computed from parameter NAMES only (src/rdw.tcl:637-641, `[lindex $pv 0]`), so alignment did not move. (f) R2's integration survives: `_row_param`'s regexp `^[ ]+(\S+)[ ]+:` reads only the name, and I drove `_reslot_block` over an engineered block and got a clean permutation (`gds : 4.7p / id : 11.1u / gm : 1m`). (g) Idempotence is safe - a rendered value fed back in is not a double and passes verbatim (`11.1u`, `1m`, `4.7p`, `9.98e-304a`, `1e+296T` all unchanged; only `-0` -> `0`, and no code path re-formats). (h) Cost is negligible: 2000 `_value_text` calls in 12.5 ms (6.3 us each) on this box. (i) Empty `devices`, and a device with an empty pair list, both render correctly. (j) Tcl here is 8.6.17, so `1_000` is not a double and takes the verbatim arm (an 8.7 tree would rebase it - latent only). (k) The four blockers the implementer declared are all true as stated: issue 1330's file still opens "Status: FILED, NOT FIXED" while the code is fixed, issue 1331 is untouched and off this item's path, the two :0 suite debts still stand, and the `1341_nonfinite_in_the_devices_bucket` rule debt is filed with --eyes.

########## VERIFY #5 :: REFUTED

 * **REFUTES — THE EMPTY WINDOW'S COPY DESTROYS THE USER'S CLIPBOARD, WHICH IS THE EXACT HARM CP5 EXISTS TO PREVENT, ARRIVING THROUGH THE DOOR CP4 TESTS.** `Tools > Results Display Window` (/home/analog/dev/xschem-claude/src/xschem.tcl:17638) calls `rdw::open` with no dumps. Right-click -> Select All -> Copy, three clicks, no simulation. `rdw::select_all` (/home/analog/dev/xschem-claude/src/rdw.tcl) does `tag add sel 1.0 end`; on an EMPTY Tk Text that is the range `{1.0 2.0}` — the widget's mandatory trailing newline — so its guard `if {[llength $r] < 2}` NEVER FIRES. `rdw::copy` then finds a one-character span, and its own empty guard `if {$txt eq {}}` cannot fire either because the character is "\n". Measured, fresh session, on :99 AND on the user's VcXsrv, identically: clipboard `MY-IMPORTANT-DOCUMENT-TEXT` -> a bare newline; status line says "Selected the whole window, 1 line." then "Copied 2 lines, 1 characters, to the clipboard." Three false statements and a wiped clipboard. Pre-R3 the same session leaves the clipboard untouched (measured on the copy: `clipboard preserved`), so this is NEW harm from R3's own new code. CP4 exercises Select All only on a POPULATED fixture; CP5 exercises the empty-selection guard only via the chord and only after `tag remove sel`. Neither row can see it. Repro: /tmp/claude-1000/-home-analog-dev-xschem-claude/1e23e38a-228a-491e-a9b0-387bda9d283e/scratchpad/probe_r3c.tcl

 * **REFUTES — CTRL-C COPIES THE PANE INSTEAD OF WHAT THE USER SELECTED, WHENEVER THEY SELECT IN THIS WINDOW'S OWN STATUS LINE.** `.rdw.s.msg` (/home/analog/dev/xschem-claude/src/rdw.tcl:1362) is an `entry`, `-state readonly`, exportselection 1 — selectable by drag (driven with real ButtonPress/B1-Motion/ButtonRelease; `selection present` -> 1, `sel.first`/`sel.last` -> 0/39) — and it is where item B5 puts the settings-file path, i.e. the single most copy-worthy string in the window. Selecting there takes PRIMARY off the pane. `rdw::_selection_changed` tests only `$own eq {.rdw.p.t}`, so a LOCAL widget of this same toplevel is scored as a foreign theft and the mirror is KEPT. The chord is now on the `.rdw` bindtag, which that entry carries. MEASURED on :99 and on the user's own VcXsrv, byte-identical: PRIMARY holds `/home/analog/.xschem/op_param_lists.tcl`, the user presses Ctrl-C, the clipboard receives `MCU:/` — the pane's stale line — and the status line announces "Copied 1 line, 5 characters, to the clipboard." Pre-R3 the same gesture left the clipboard at its sentinel (measured on the copy). R3 converts "copied nothing" into "silently handed you the wrong text", which is CP12's own stated defect class in a shape no CP row looks at. Repro: scratchpad/probe_r3b.tcl section S.

 * **AND ITS SECOND HALF: the same Ctrl-C, with no pane selection standing, DESTROYS THE TEXT THE USER IS MID-COPY OF.** `rdw::copy`'s no-selection branch calls `rdw::status`, which writes `::rdw::statusmsg` — the `-textvariable` of the very entry holding the live selection. Measured: status `/home/analog/.xschem/op_param_lists.tcl` -> "Nothing is selected, so the clipboard was left alone. …" The path vanishes under the user's own selection, and the sentence is false: something WAS selected, in this window, and it is on PRIMARY.

 * **A COST THAT IS NOT RECORDED AS A DECISION: after a PRIMARY theft the selection cannot be put down.** Post-theft `sel` is already empty, so an ordinary click in the pane changes no tag, fires no `<<Selection>>`, and `keepsel` stays lit. Driven: selected line 2 (`@m.x1.mcu`), let another client take PRIMARY, clicked line 5, pressed Ctrl-C -> clipboard `@m.x1.mcu`. On VcXsrv — where the commit's own argument is that thefts happen on the bridge's schedule — that is the ordinary state, and the only way to clear the highlight is to make a new selection. Recorded as a look debt, not fixed.

 * **A SECOND UNRECORDED COST: a drag that starts inside a standing selection can only GROW it, never shrink it.** `rdw::pane_drag` unions the drag's answer with `dragfrom` unconditionally, so the drag's own result is swallowed whole. Driven with real events: selection `{3.0 3.121}`, press at 3.5, drag to 3.15, release -> selection still `{3.0 3.121}` — the drag does literally nothing. To select a word inside a line you have already selected, you must first click OUTSIDE. CP7 legs 4-5 fence only the press that lands outside; the inside-shrink case is unmeasured. Given the user's report is "selection … not reliably", shipping a mode where dragging inside a selection is a visible no-op is a plausible next bug report. Recorded as a look debt, not fixed.

 * **A DEAD GUARD AND A SELF-CONTRADICTING PAIR OF SENTENCES.** `rdw::copy`'s `if {$txt eq {}}` cannot fire for any span this window can produce (`get first last` with first<last always yields >=1 char), so the real "nothing worth copying" case — a whitespace-only span — is unguarded; the empty window above is its reachable instance. And for one and the same content the window says "Selected the whole window, 1 line" and then "Copied 2 lines, 1 characters" — `select_all` counts `end - 1c` lines, `copy` counts `split $txt \n` elements. That is inside rule debt 1339_R3_copy_says_what_it_did but the disagreement is arithmetic, not wording.

 * **A MEASUREMENT IN THE IMPLEMENTER'S TABLE THAT DID NOT REPRODUCE.** Their $DISPLAY cell reads "5 FAILED (66 passed) … The 5 remaining reds are RA1..RA5". My two runs on that same server gave 6 FAILED (SD3b + RA1..RA5) and 7 FAILED (SD2 + SD3b + RA1..RA5). Issue 1332's `after 100` flake, recorded as "once in 134 runs" on :99, fired on 2 of 2 runs on the user's real server. Their blocker list says 1332 "was not seen in any of the eight runs taken here" — on VcXsrv it is not rare, and any future $DISPLAY count for this suite is unusable until 1332 is fixed by polling.

 * **NOT R3's, CONFIRMED STANDING.** RA1..RA5 (item R4's raise, issue 1343) fail on the user's VcXsrv in both of my runs and pass on :99 — I did not re-derive the pre-existence proof, the implementer's is on record. Issue 1331 untouched. Issue 1330's file still opens "Status: FILED, NOT FIXED" although R2 fixed the code — now the fifth item in a row to record that rather than fix one line.

 * **WHAT SURVIVED THE ATTACK, and it is most of the item.** The toplevel-tag chord genuinely fixes "the keyboard is not in the pane"; the PRIMARY-theft mirror genuinely restores both the highlight and the copy; the double-click-then-drag union genuinely keeps the word whole, 3/3 with no gap between gestures as well as 5/5 with one; the canvas keeps its own Ctrl-C; the three procs are safe with the window closed; and the six new rows really are red on the pre-R3 source. The refutation is not that the mechanism is wrong — it is that two ordinary gestures in the same window now put the WRONG text, or no text, on the clipboard, and the suite's twelve green CP rows cannot see either.
