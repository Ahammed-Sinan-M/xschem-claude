### 1244_A3_declutter_per_pdk
annotation + declutter on, per PDK: an annotated FET should draw its name and its OP block and nothing else. Suites green (82 checks incl. rows A27/A28 on the real sky130A / gf180mcuD / ihp-sg13g2 symbols); please look at the pixels.

### 1244 declutter survives Ctrl-6? (on screen)
RULING D-8 makes the declutter a bit on annot_show, so Ctrl-6 clears it with everything else. Measured, not decided: after 6 -> Ctrl-Alt-6 -> Ctrl-6 -> 6 the parameters are back and Ctrl-Alt-6 must be pressed again. That follows from D-8 and is left to be judged on screen rather than argued about. (Judgeable only once item A3 makes something actually disappear.)

### 1338 R2: the row moves under your eyes
Up/Down now move the cursored row in the Results Display Window and the shading follows it to the row's new line, and every OTHER block of the same class re-orders with it. Suites green (124 --nogui / 136 :99 window, 53 keys :99, 130 store, 485 op_annot control, T1 zero); please look: does the row visibly move where you expect, does the shading land on it and not on the line it left, and is an OLDER dump re-ordering under you acceptable (that half is rule debt 1338_R2_every_block_of_the_class_follows).

### 1338_R2_window_vs_sheet_disagree
ADVERSARY FINDING on item R2, MEASURED, not a pixel guess - but the ACCEPTABILITY is yours. (1) SUMMARY LIST (key 2): two Up presses move the pane from ids/gds/gm to gds/ids/gm while op_annot::text M1 stays BYTE-IDENTICAL at id/gm/gds, before and after, on :99 with a live pane. The sheet CANNOT follow a summary reorder: op_param_lists::_show_set builds shown by filtering the union with the ANNOTATION list, and op_annot::text draws shown in shown order, so the summary order never reaches the sheet. The status line says a plain Up: moved gds up in the summary list for class X, with no hint. Row RE7 passes because it counts annot_overlay_flushes, which goes +1 on a no-op re-register. So for key 2 the window now CONTRADICTS the schematic where before R2 both stood still. (2) A NEW DUMP AFTER A REORDER puts one class on screen in TWO orders at once - the exact thing rdw::_reorder_shown says it exists to stop and RE5s title claims: after two Ups the old M1 block reads gds/ids/gm and a fresh dump of the SAME device lands above it reading ids/gds/gm, because rdw::push does not re-slot. Older dumps carry the NEW order and the newest dump carries the raw one. (3) A FLAVOR (narrow) reorder, whose status line explicitly says it wrote only for cells matching one glob, still reshuffles the block of a DIFFERENT cell of the class from gm/ids to ids/gm. (4) After Delete then Add the pane and the store disagree again - store ids/gm/gds, pane ids/gds/gm - and nothing re-slots or says so. JUDGE: is the window allowed to show an order the sheet does not draw, and are (2),(3),(4) acceptable or is the answer to re-slot at rdw::push and after every edit rather than only on Up/Down.

### 1339_R3_select_and_copy
ITEM R3 (issue 1339): select a block in the Results Display Window and press Ctrl-C, then paste it somewhere. Suites green -- ALL PASS 71 on :99 AND every row of section CP green on YOUR OWN VcXsrv server ($DISPLAY 172.20.160.1:0, vendor HC-Consult, ruling DD-8) -- please look at the pixels. Four things want eyes and no suite can judge them: (1) after another application takes the X selection the highlight in the pane is now drawn by a tag of the window's own, so CHECK IT STILL LOOKS LIKE A SELECTION and not like a second, different colour; (2) the right-click Copy / Select All menu, which is new, and whether it reads right in your theme; (3) double-click a word, LET GO, then press and drag -- the word should stay whole and the selection should grow, which is the gesture you said worked exactly once; (4) the two new status sentences (see rule debt 1339_R3_copy_says_what_it_did).

### 1339_R3_selection_you_cannot_put_down
ITEM R3's ADVERSARY, MEASURED ON :99 AND ON YOUR OWN VcXsrv, NO ROW FENCES EITHER. Two gesture costs R3's mirror introduces, both judgeable only by hand. (1) AFTER ANOTHER APPLICATION TAKES THE X SELECTION the kept highlight cannot be put down: clicking another line in the pane leaves it lit and a following Ctrl-C still copies the OLD span (driven: selected line 2 '@m.x1.mcu', let another client take PRIMARY, clicked line 5, Ctrl-C -> clipboard '@m.x1.mcu'). On VcXsrv, where thefts are routine, that is the ordinary state. (2) A DRAG THAT STARTS INSIDE A STANDING SELECTION CAN ONLY GROW IT: select a whole line, press inside it and drag across one word and the selection does not move at all (driven: {3.0 3.121} before and after). To make a smaller selection you must first click outside the current one. Both follow from the design and neither is recorded as a decision. Please judge on your own screen.

### 1340_R4_adversary_the_keyboard_after_a_dump
ADVERSARY FINDING on item R4 (issue 1340), MEASURED on :99/openbox, before-and-after on the SAME binary via XSCHEM_SHAREDIR against a HEAD~1 worktree. THE COMMIT SAYS the re-map's keyboard grab is 'Fixed by arming the EXISTING one-shot hand-back (rdw::_arm_focus_handback)'. IT IS NOT: that hand-back NEVER FIRES once you have clicked in the pane even once - which is the one thing the window exists for (issue 1306). Tk remembers .rdw.p.t as the toplevel's focus child, so the window manager's map-time grant is routed to the CHILD; rdw::_focus_handback demands EXACT equality with .rdw and declines. Traced: FocusIn .rdw detail=NotifyVirtual with [focus] ALREADY .rdw.p.t, _focus_handback entered three times with pending=1, declining each time. CONSEQUENCES MEASURED, 5/5 and 3/3 deterministic: (a) on a bare rdw::push the keyboard ENDS IN THE WINDOW and STAYS there (pre-R4: stays on the canvas); (b) ::rdw::focus_pending is left ARMED AT 1 FOREVER, including on the shipped key path - the exact 'flag left lying around is a bounce waiting to happen' its own comment warns of, and it DOES bounce: force a later landing on .rdw and the keyboard is yanked to the canvas; (c) the only thing keeping the keyboard on the schematic on the shipped key/pick path is rdw::show's SYNCHRONOUS focus -force, winning by ~0.4-0.8 ms (traced: _raise returns 1435.6 ms, the WM grant lands 1436.4 ms, _focus_canvas runs at +0.1 ms) - and this file's own comment says a synchronous hand-back CANNOT win that race. Stub _focus_canvas out and the keyboard is in the window 3/3. (d) THE SUITE IS BLIND: section RA's ra_fixture never clicks the pane. Add exactly one click to RA1's own fixture and RA1's focus leg reads .rdw.p.t where the row expects .drw - measured; pre-R4 the same fixture reads .drw. WHAT ONLY YOU CAN SETTLE, on YOUR VcXsrv server (the one this item exists for, where the re-map is required and :99's plain raise is inert): annotate a sheet (key 6), press 1 over a FET, CLICK IN THE RESULTS PANE as if to copy the text, click back on the schematic, select another device and press 1. THEN, WITHOUT CLICKING THE SCHEMATIC, press 1/2/3/4 and Escape. If they do nothing, the keyboard is in the Results window and this is the half you forbade in the same sentence as the request. A 0.7 ms margin on a local Xvfb is not evidence for a server reached over TCP.

### 1341_R5_the_window_says_did_not_converge_when_the_formatter_fails
ADVERSARY FINDING ON ITEM R5 (issue 1341), MEASURED AND DRIVEN IN A LIVE PANE ON :99 - PLEASE LOOK BEFORE YOU JUDGE THE FORMAT, because if you hit it you will mis-judge the whole item. rdw::_value_text now decides 'this value is non-finite' by seeing an EMPTY answer come back from op_annot::eng_or_blank. That proc returns empty for TWO different reasons: the value really is nan/inf, OR to_eng could not format a perfectly good number. The second reason is reachable from a shipped menu: Simulation > Set netlist / graph / annotation precision is a free-text entry with NO validation, and anything that is not a plain non-negative integer sticks - -1, 2.5, abc, 4x, +4, 0x4, 6. all do. From that moment every finite MEASURED value in the Results Display Window prints '(did not converge)'. Driven: same device, same numbers, same raw, two dumps in one session - the first block reads 'id : 11.1u / gm : 1m / vth : 0.75', then precision set to -1, and the second block reads 'id : (did not converge)' three times. Already-pushed blocks keep their real numbers, so the pane shows a MIXTURE and it reads exactly like a circuit that stopped converging. AND IT BREAKS R5'S OWN HEADLINE PROMISE: in that state the sheet blanks the row (op_annot.tcl:2125 renders 'id =') while the window asserts non-convergence, so the two surfaces DO disagree - which is the one thing this item exists to stop. No suite row sees it (EN6 drives precision 4 and 6 only). Before R5 the window printed the true raw value whatever the precision was. WHAT ONLY YOU CAN SETTLE: should the window EVER print words where the sheet prints a blank, and is '(did not converge)' acceptable as the answer to 'the formatter failed'? Reported to the driver as a refutation; the driver decides revert or repair (op_annot::_finite is already the discriminator eng_or_blank itself uses).

### 1343_rdw_raise_on_your_own_server
ISSUE 1343, FOUND WHILE PAYING DD-8 AND NOT FIXED. Item R4's raise (issue 1340, closed FIXED) is ALL PASS on Xvfb and FIVE OF SIX ROWS FAIL on the server you actually look at -- $DISPLAY 172.20.160.1:0, vendor HC-Consult, the VcXsrv your report came from. Proved pre-existing: the same five reds appear byte-identically with item R3's source change reverted. Please do the thing you asked for and watch: press 1 over a selected device with the Results window open but buried, and say whether it comes to the front on YOUR screen. And press Ctrl-Alt-S for the Library Manager -- if THAT does not raise either, 1343 is issue 0054's and older than this batch.

### 1344_rdw_copy_after_repair
empty window Select All+Copy must not touch your clipboard; Ctrl-C on the settings path in the status line must hand you the path and leave it on screen

### 1344_the_status_line_receipt_goes_stale
MEASURED on :99 and on your VcXsrv, identical, four ordinary gestures and no simulation. Right-click Select All, Copy -- the bottom line says 'Copied 6 lines, 169 characters, to the clipboard.' Now drag across that sentence and press Ctrl-C. The clipboard really does become that 49-character sentence, and the bottom line STILL reads 'Copied 6 lines, 169 characters, to the clipboard.' The driver decision filed as rule debt 1344 says the window 'says nothing' when the thing being copied IS the status line; what it actually does is keep the PREVIOUS sentence, and that sentence is usually a receipt for a different copy. Worse shape, three gestures: press Ctrl-C with nothing selected -> 'Nothing is selected, so the clipboard was left alone.'; drag across that sentence; Ctrl-C -> the clipboard now HOLDS it while the line still says the clipboard was left alone. Please look and rule: silence is fine only if the line is also CLEARED, or the receipt must be shown somewhere that is not the text being copied.

### 1344_two_highlights_after_a_status_line_drag
MEASURED on :99 and on your VcXsrv, identical. After another X client takes PRIMARY (the VcXsrv bridge does this on its own schedule), the RDW keeps the pane's selection painted as keepsel -- that is item R3's whole point. If you then drag across the settings-file path in the status line, BOTH highlights are on screen at once: the pane still shows line 2 selected AND the status line shows its own selection. Ctrl-C copies the STATUS LINE. Then put the status line's selection down with an ordinary click and the SAME chord silently goes back to copying the pane's highlight -- no event, no message, nothing on screen changed except one highlight going out. The repair chose to REFUSE the mirror inside rdw::_selection_span rather than drop it, so nothing repaints. Please look and say whether two selection-coloured regions in one window is acceptable, or whether the pane's highlight must go out the moment a sibling takes the selection.

### 1349_Delete_and_Add_now_wipe_the_pane_selection
ADVERSARY FINDING on the P4 repair (commit f44a982a), MEASURED on :99 and proved NEW by restoring src/rdw.tcl from a1e85d3c. The 1349 fix adds rdw::render_pane to the Delete/Add arm of rdw::button. render_pane does 'delete 1.0 end' and rdw::_forget_selection, so a selection standing in the pane is destroyed by every Delete and every Add. MEASURED, same fixture, same gesture: sel {16.0 17.14} and 28 bytes on X PRIMARY survive a Delete at a1e85d3c and are sel {} / 4 bytes at f44a982a. It fires even when the re-slot changed NOTHING - blocks byte-identical across the Delete in the after-an-Up case - so the user loses the highlight they were mid-copy of for a repaint that redrew the same text. This window exists for select-and-copy (item R3, DD-5), CP12 fences only the NEW DUMP repaint, and no row in section RE or CP holds a selection across a button press. JUDGE: on screen, does losing the highlight on Delete/Add read as acceptable, or should the repaint be conditional on the blocks actually changing?

### 1349_the_pane_order_flips_between_lists_on_Add
ADVERSARY FINDING on the P4 repair (commit f44a982a), MEASURED on :99 with a live pane and proved NEW by restoring src/rdw.tcl from a1e85d3c. On list 2 the user reorders the summary list: the pane follows (ids gds gm -> gds ids gm) and the window says so. Add is greyed on list 1, so the ONLY place Add is pressable next to a summary reorder is list 2 - and rdw::button's deflist sends an Add on list 2 to the ANNOTATION list. The 1349 re-slot then re-slots the pane by the ANNOTATION order: the M1 block snaps gds ids gm -> ids gm gds while listkind is still summary, and the status line says only 'Add: added gds to the annotation list for class p4cls.' Pre-repair the pane kept the summary order (measured: pane==summary 1 before, 0 after). So the pane and the SUMMARY store now disagree about order - the exact property 1349 was fixed to keep - and it disagrees one press after rdw::_drawn_note told the user the window is the half that follows list 2. JUDGE: is the pane allowed to change which list's order it is showing, with nothing said?

### 1369_the_keyboard_after_a_dump_on_your_own_server
Issue 1369 landed: the dump's hand-back now asks which TOPLEVEL the keyboard landed in (winfo toplevel), not whether it landed exactly on .rdw, so a click in the results pane or on the status line no longer disables it for the rest of the session; a press anywhere in the window spends the one-shot instead. Suites green (keys 90 x5, window 184 --nogui / 216 on a private Xvfb, store 130, seam 49) and three sabotages red by name. THE HALF NO SUITE HERE CAN REACH IS YOURS: on :99 the dump path's own focus -force beats openbox's map-time grant, so the grant never lands and the end-to-end symptom cannot be reproduced. Please do exactly what you reported: annotate, press 1 over a FET, CLICK IN THE RESULTS PANE as if to copy, click back on the schematic, select another device and press 1 - does the second device's OP info now reach the window on the FIRST click? And without clicking the schematic first, do 1/2/3/4 and Escape still work? This also answers the standing debt 1340_R4_adversary_the_keyboard_after_a_dump.

### 1371 the Case chooser and the widened row editor
Issue 1371's door, repaired after an adversary refuted it. Suites green (test_ase_simdlg_0937 47 on :99 / 5 --nogui, simcaps 84, simreg 83, window 228, dialogs 176, persist 136, probe 44, casemode_registry 28, run_profile 37, launch 44, preflight 114) and eight sabotages red by name -- but no check can measure a pixel. Setup > Simulators... -> click a row -> Edit...: (1) does the read-only Case: combobox look read-only in your theme; (2) the editor grew from three grid rows to six -- is the Detect button clipped, and does the dialog still size sensibly; (3) do the marked labels fit -- 'preserve (not tried yet)' and 'preserve (NOT supported)', which are the two states a mode outside the offer can be in; (4) the status line under the fields now carries a sentence the moment the editor opens (press-Detect on a cold program, the measured list on a warm one) -- is that line legible and not clipped at 420px wraplength. These three questions were filed inside the SUITE debt for test_ase_simdlg_0937, which drain clears on a green run; they are here now because a green suite answers none of them.

### DD-5 analysis sentence in the Results Display Window
The exact wording, on screen: 'These numbers come from the first point of results xschem reports as a dc analysis, not as a standalone operating point. A dc sweep's first point is one sweep step, and xschem also reports a multi-point operating point as dc.' It is longer than the driver's own specimen because the specimen was REFUTED by a measurement -- save.c renames a multi-point Operating Point plot to dc, so the short version would tell someone who ran only an operating point that they ran a sweep. Suites green, please look.

### DD-6 narrowing on the schematic
The DD-6 display key (shown) landed: op_annot::text now draws 'shown' when a descriptor carries it and 'params' otherwise, and op_param_lists::apply writes BOTH (the union into params, the annotation list into shown). NOTHING CALLS apply YET, so this is invisible until item B5 wires the button -- but the moment it does, the on-sheet block SHRINKS and the declutter gate's answer changes with it. Suites green (test_op_annot 485/492 and test_annot_declutter_1244 134 all UNMOVED); please look at a decluttered sheet once B5 lands. Paired with ledger entry 1245_DD6_display_field.

### balloon tooltips near a screen edge, tree-wide
issue 1368 changed the SHARED balloon_show: a tip that would not fit is now MOVED - a widget-anchored one slides and flips above its widget, a pointer-anchored one mirrors to the other side of the pointer. 44 call sites across six files. Measured fix for the file browser's two directory tooltips, which never appeared at all when the pointer sat in the rightmost ~555px. A tip that already fits is still not moved. Please look at a few tooltips near your own screen edges.

### op_1364_annotate_on_your_own_bench
Please confirm the fix on YOUR bench, with YOUR ngspice-ver50, through the gesture you actually use -- I could not: every measurement here is Xvfb and a scratch HOME. Open sky130_tests_ase/tb_bandgap, descend x1 -> x1, run, then annotate M18 the way you normally do (6, or Simulation > Graphs > Annotate Operating Point into schematic, or a launcher button on the sheet). BEFORE this fix that path rendered 'id' and left gm/gds/vgs/vth/vds BLANK on your registry; it should now render all six. The numbers I measured on the smaller committed cell test_nfet_final were id 409.7u / gm 503.3u / gds 69.57u / vgs 1.737 / vth 0.7813 / vds 0.8734. A green suite cannot tell you your own design annotates.

### op_param_lists.conf grammar v2 + the file-order precedence header
Item B2c rewrote the whole header block every settings file carries, and the precedence rule it states is now TRUE of the code that emits it (row F5 reads the sentence back out of a freshly written file and builds the case it describes). THE FILE TO READ: <project>/.xschem/op_param_lists.conf -- write any flavor row, save, open it. THE SENTENCE TO JUDGE: 'PRECEDENCE among `flavor` rows: when two globs of the SAME class both match a cell name, THE FIRST ONE IN THIS FILE WINS. Nothing is ranked and nothing is measured for narrowness: put the row you want to win ABOVE the other one.' plus its worked e.g. line, the 'a flavor row answers ONLY for the class named in its own row' line, the cross-tier line ('your personal file is read BEFORE this project's'), and the new closing line 'xschem edits only the rows it changed and leaves everything else in this file exactly as you wrote it'. SUPERSEDES look debt [op_param_lists.conf_precedence_comment], which describes B2a-2's REFUTED narrowness ranking -- that sentence is gone from the tree. Suites green (test_op_param_store 79 checks, test_op_annot 485/492, test_annot_declutter_1244 134 unmoved); please look.

### op_param_lists.conf precedence comment
B2a-2 rewrote the seven comment lines every settings file carries about flavor-glob precedence. Suites green; please READ THE EMITTED FILE and say whether the sentence is clear to a teammate who receives it: write any flavor row, save, and open <project>/.xschem/op_param_lists.conf. Paired with rule debt 1277_precedence_sentence.

### rdw_1300_narrowed_pane
Issue 1300's narrowed pane, on your own tb_bandgap. Suites green (window 155/167, keys 83, store 130, op_annot 485) - please LOOK. Press 1 over M18 in /x1/x1: you should see six rows (id gm gds vgs vth vds) under one new sentence naming the mos annotation list and saying 82 columns were withheld; press 3 and get all 88 back with no such sentence; press 2 and see the same six rows under a sentence naming the SUMMARY list. Three things only eyes can judge: whether the new sentence is too long to sit above six numbers, whether saying the withheld COUNT is worth the line, and whether keys 1 and 2 showing identical ROWS under different sentences reads as a bug to you (it is issue 1353's open E question, not a defect in the narrowing).

### rdw_1355_chrome_and_dialog
The Results window gained TWO surfaces that only your eyes can settle, and the suites being green says nothing about either. (1) A NEW LINE ABOVE THE PANE, full width, above both the pane and the buttons - 'Keys 1/2/3: the summary list (computed, not drawn) - the buttons edit this list, not the block you are reading.' MEASURED: it costs 25 px of height and does NOT widen the window (label 763-807 px against the window's own 893), so nothing moved. Is it in the right place, is it the right length, and does it read as chrome rather than as part of the dump? (2) THE POP-UP now carries a statement line where list 3 already asks its question: 'This changes the summary list (computed, not drawn).' Dialog request width 423 px. Is the sentence in the right slot, and does the Add-from-summary variant - which is two sentences and 137 characters - read or crowd? (3) THE TITLE now reads 'Results Display Window - the summary list'; check what your window manager does with it. Reproduce: press 1, 2 and 3 over a device in tb_bandgap /x1/x1 and watch all three surfaces move together; then press Delete.

### rdw_1356_note_cut_off_measured
ADVERSARY MEASUREMENT, and it settles the 'very likely' in rdw_1356_selection_note: issue 1356's clause IS cut off at the window's own default width, deterministically. Driven on the user's own M18:/x1/x1 of tb_bandgap with their own raw and .opinfo, on a private Xvfb :98 with openbox 3.6.1, 1920x1080x24: a real mouse drag across all six summary rows then a real Delete press produces a 147-character status line measuring 1045 px in the entry's own TkTextFont against an entry 887 px wide at the window's default 893x498 geometry. VISIBLE: 'Delete: removed id from the summary list for class mos. Selecting lines does not choose them for editing - the buttons act on'. CUT OFF: ' the shaded row alone.' So the reader is left with a dangling 'the buttons act on' and the ANSWER - which row the buttons act on - is off-screen, with no scrollbar, no xscrollcommand, and xview parked at 0.0-0.85. The window has to be widened to 1051 px (+158, +18 percent) before the sentence fits. AT THE SAME DEFAULT WIDTH THE PRE-FIX BUILD's status for the identical gesture was 55 characters and 398 px and fitted with 489 px to spare, so this is new with the 1356 clause. TWO PRE-EXISTING SENTENCES OVERFLOW THE SAME ENTRY and are the same placement question: 'Delete: no row is marked in this window...' at 893 px (cuts 'n.') and 'Delete: X is not in the mos summary list. The pane also shows rows...' at 1082 px (cuts 'wn rows can be edited here.'). Your call whether the clause moves off the one-line entry (a second line, the chrome label, or a wrapped message) or whether the window should simply open wider.

### rdw_1356_selection_note
Issue 1356's clause is appended to a one-line sunken status entry at the bottom edge of the Results window, and the entry is 887 px of DejaVu Sans 10 at the window's default width - MEASURED earlier in this batch as about 128 of 158 characters visible. The full sentence after a Delete made with six rows highlighted is 'Delete: removed id from the summary list for class mos. Selecting lines does not choose them for editing - the buttons act on the shaded row alone.' - 148 characters, so the tail is very likely scrolled off on a default-width window. The important half leads and is visible; the half that answers the confusion may not be. Reproduce: drag-select several rows in the pane, press Delete, and see how much of the line you can actually read. If the tail is unreadable the fix is a placement question, not a wording one, and it is yours.

### rdw_1358_keys_in_the_window
The fix is a KEYBOARD change and no eyes have been on it. Please, on your own screen: open the Results window over a MOSFET, press 2, CLICK a parameter row, press Delete, accept the pop-up defaults, then press 2 WITHOUT touching the canvas - a new block should appear narrowed to the list you just changed, with the deleted row gone from it. Two things to judge that a suite cannot: (a) the older block above it still shows the row you deleted, correctly, because each block is a past-tense record - tell us if that reads as 'the delete did not work' anyway; (b) after that press the keyboard is handed back to the canvas (shipped behaviour of every dump), so your window may lose its active-title decoration under your WM - tell us if that is disorienting on 172.20.160.1:0.

### rdw_1361_chrome_width_measured
THE SUMMARY LIST'S CHROME LINE IS NOW 30 CHARACTERS LONGER AND THE WINDOW GROWS WITH IT - THE ONLY PIXEL CONSEQUENCE OF ISSUE 1361, AND IT IS YOURS. The summary line gained 'Add writes the annotation list.' because without it the label was FALSE (an Add made from list 2 writes the annotation list, spec 4.2 B7). MEASURED on the dev display :99 (Xvfb 1920x1080x24, openbox 3.6.1, GUI_GATE=0), reading winfo reqwidth off the live label and wm geometry off the live toplevel, non-cadence wording: annotation label 744 px / window 893x498; summary label 963 px / window 971x498; list 3 label 723 px / window 893x498. So THE WINDOW GROWS 78 px WHEN YOU PRESS 2 AND SHRINKS BACK WHEN YOU PRESS 1, until you resize it by hand. In the cadence profile the summary label measures 978 px, so that window would be about 986. SIDE EFFECT, and it points the same way as your other look debt: the status entry .rdw.s.msg grows 887 -> 965 px on the summary list, which is still 80 px short of the 1045 px issue 1356's own Delete sentence needs (rdw_1356_note_cut_off_measured). THREE OPTIONS: (a) accept the resize - it is honest, and on list 2 it also makes the status line 78 px less truncated; (b) pin the chrome label to the widest of the three sentences so the window never jumps, at the cost of dead space on lists 1 and 3; (c) shorten the exception - 'Add writes list 1.' fits inside today's 893 px, but 'list 1' is meaningless in the profile where the line says 'Showing' because there are no 1/2/3 keys. NOT MEASURED ON YOUR OWN SERVER: 172.20.160.1:0 (vendor HC-Consult) may substitute a different font, so every px number here is Xvfb's TkDefaultFont. Suites green (window 173 --nogui / 190 :99, keys 88 :99), please look.

### rdw_1362_status_wrap
THE PIXEL HALF OF ISSUE 1362 IS UNPAID AND ONLY YOU CAN PAY IT. Everything below was measured on Xvfb :99 with openbox 3.6.1, where TkTextFont resolves to DejaVu Sans 10; your own server is $DISPLAY 172.20.160.1:0 (vendor HC-Consult) and may substitute a different font. THE FIX DOES NOT DEPEND ON THE NUMBER -- rdw::_status_show asks the live widget 'count -displaylines', so it adapts to whatever font you have -- but WHAT IT LOOKS LIKE is yours. WHAT TO DO: open the RDW over M18:/x1/x1 of tb_bandgap, press 2, drag across several parameter rows, press Delete, accept the defaults. WHAT TO CHECK: (a) the whole verdict is readable, ending in 'the buttons act on the shaded row alone.' -- on :99 it wraps onto 2 lines and the last clause is fully visible; (b) the window grows from 893x498 to 893x515 while that verdict stands and shrinks back on the next short message -- is that acceptable, or should the window stay one size and the pane give the line up? (rule debt 1362); (c) at rest the status field is 887x21 and the window 893x498, byte-identical to the old one-line entry -- confirm nothing looks different when the messages are short; (d) select the settings-file path in the status field with the mouse and press Ctrl-C -- the path must arrive with NO trailing newline (the text widget's mandatory one is clamped away); (e) drag the window narrower and confirm a long verdict re-wraps and still reads in full. A GREEN SUITE DOES NOT DISCHARGE THIS.

### rdw_1365_status_scroll
THE PIXEL HALF OF ISSUES 1362 AND 1365, AND IT SUPERSEDES look debt rdw_1362_status_wrap -- please read this one instead of that one. ⚠ TWO OF THAT ENTRY'S CHECKS ARE NOW VOID: its (a) said a long verdict 'wraps onto 2 lines and the last clause is fully visible' and its (b) said the window grows '893x498 to 893x515'. Measured since: an ordinary composed Delete verdict is 618-676 characters and needs 6-7 lines, not 2; the four-line cap is 893x549, not 515; and until issue 1365 anything past 491 characters was ELIDED in the widget with a '...', which also meant a select-and-copy handed you the elided text. All of that is fixed; none of it has been seen on YOUR server. Everything below was measured on Xvfb :99 with openbox 3.6.1 where TkTextFont resolves to DejaVu Sans 10; your own server is DISPLAY 172.20.160.1:0 (vendor HC-Consult) and may substitute a different font. THE CODE DOES NOT DEPEND ON THE NUMBERS -- rdw::_status_show asks the live widget 'count -displaylines' -- but WHAT IT LOOKS LIKE is yours. WHAT TO DO: open the RDW over M18:/x1/x1 of tb_bandgap, press 2, drag across several parameter rows, press Delete, accept the defaults. WHAT TO CHECK: (a) the verdict is READABLE and, if it runs past four lines, a SCROLLBAR is beside the status field -- is that scrollbar visible enough to be an invitation, or does the tail look lost? (b) select the whole status field with the mouse and press Ctrl-C: the WHOLE sentence must arrive, ending 'the buttons act on the shaded row alone.', with no '...' and no trailing newline. This is the defect that made issue 1365 -- please confirm it on your own clipboard. (c) the window grows 893x498 -> 515 -> 532 -> 549 as the verdict needs one to four lines and shrinks back on the next short message: acceptable, or should it stay one size and the pane give the line up? (rule debt 1362). (d) at rest the status field is 887x21 with NO scrollbar and the window is 893x498, byte-identical to the old one-line entry -- confirm nothing looks different when messages are short. (e) drag the window narrower with a long verdict standing AND a selection standing in it: the verdict must re-wrap, still read in full, and YOUR SELECTION MUST SURVIVE (a three-pixel drag used to destroy it). A GREEN SUITE DOES NOT DISCHARGE THIS.

### rdw_1374_preamble
the RDW preamble on the user's own M18: 311 chars over 4 display lines is now 123 over 2; suites green

### rdw_keys_B4
NOTHING TO LOOK AT YET - AND THAT IS THE POINT. Item B4-2 was the SECOND attempt at these keys and it was REFUTED AND REVERTED (2026-09-04), like B4 before it. src/rdw.tcl and src/cadence_style_rc are byte-identical to 735ea26e again, so bare 1/2/3/4 still reach C logic_set and the Results Display Window is reachable only from Tools. THE WORK IS PRESERVED, not lost: doc/claude/op_param_batch/B4-2_working_tree_REVERTED.patch (B4 plus B4-2 five fixes) re-applies clean in both directions. WHAT THE NEXT ATTEMPT (B4-3) MUST SHOW YOU, kept here so the list is ready: (a) is ONE CIW line enough of a mode indicator (issue 1302 - ASE Direct Plot keeps a status-line prompt, and B4-3 may not edit ase_window.tcl)? (b) THE FOCUS RULE, still the one thing only eyes can settle: should the Results window take the keyboard when a dump opens it, or should the keyboard stay on the canvas so bare 1-4 and ESC keep working and you must click the window before Ctrl-C? B4-2 chose the canvas and its hand-back then BOUNCED a deliberate click into the text pane (issue 1306) - your ruling on (b) decides which fix is even right. (c) drag with the mode live: press, wiggle a pixel, release - NOTHING may get selected (issue 1304; measured before the fix: an 8-step drag selected 20 objects and the rubber band survived ESC). (d) click just off-grid on a device body: the window must be headed with the device UNDER THE CURSOR, not the one the grid-snapped point lands on (issue 1303; 175.175 -199.612 -> M1 vs 180 -200 -> R1). (e) the refusal sentences and the mode entry line - short enough? and a refused key must change NOTHING on screen. NO SUITES ARE GREEN FOR THIS ITEM, because it is not in the tree.

### run_cmd word order: -b before the user's args, not after
annotate's goldens move one token; fluid's order mirrors the probe

### stock xschem Simulate lost its exe/casemode composer (issue 0506)
the profile store it read is gone; ASE-L keeps everything. Re-teach simulate to read the registry, or accept the loss

### the ASE-L bottom bar's Simulator segment on the user's own screen
issue 1370: the segment is now 'Simulator: ngspice-ver50' on their bench and 'Simulator: <name> - will not run' in the four bad arms. Two things only eyes can settle: whether the em dash renders on their X server's font, and whether the 28-character segment pushes 'State:' off or widens the toplevel. Same class as the RDW chrome-width look debt.

### the B5 button column and the two scope dialogs
SUITES GREEN, PLEASE LOOK - and this item is ALL PIXELS. UPDATED IN PLACE 2026-09-04 by item B5-3, WHICH IS THE ATTEMPT THAT LANDED - the column is wired in the tree now, and this is one debt for one button column, not a second filing. EVERY COUNT IN THE PREVIOUS TEXT WAS STALE (it said 76->100, 86->111, 102->113, 35->40); TRUE COUNTS NOW: test_rdw_window_1245 83 -> 109 headless and 94 -> 121 on the dev display, test_op_param_store_1245 114 -> 130, test_rdw_keys_1245 36 -> 41 (its check-count FLOOR raised 36 -> 41, and the store and window suites each GAINED a floor of their own, 130 and 109). ALL PASS on every arm; test_op_annot UNMOVED at 485/492 and test_annot_declutter_1244 UNMOVED at 134. WHAT TO DO: annotate a sheet (key 6), press 1 over a FET to open the Results window with a dump, then click a PARAMETER ROW in the pane and press the buttons. JUDGE: (1) is 'the row your cursor is in' discoverable at all - there is no highlight, no selection bar, just the text cursor, and nhse's own rule is the precedent; (2) the SCOPE DIALOG - 'this device flavor only (<cell>)' versus 'every device of class <class>', and for a press on list 3 the SECOND question 'which list should it go into'. Is the cell name the right label for the narrow arm, or should it name the DEVICE (M1) instead? (3) the GREYING as you switch 1/2/3 - Delete greys on list 3, Add greys on list 1, and the column must not move under the pointer; (4) the status line carries TEN new sentences, all unratified - the DD-10 refusal is your OWN words verbatim, the rest are B5's; (5) a NARROW edit is stored and does NOT change the sheet (issue 1310) and the status says so - is that sentence enough, or should the narrow arm be greyed until 1310 is fixed? (6) CHANGED SINCE THE REVERTED FILING: Up/Down now REDRAW IMMEDIATELY - the sheet follows a reorder at once, because DD-13 fixed issue 1312 and the old status clause citing it was a false statement on screen. Judge that, and judge that a reorder writes at the flavor entry whose GLOB governs the device, so it can move the row for every cell that entry matches. (7) Save names the exact path it wrote in the status line. (8) AND THE SHEET ITSELF: after a Delete, look at a DECLUTTERED sheet - this is the first time apply has a functional caller, so the on-sheet block really shrinks. Paired with look debt DD-6_narrowing_on_the_schematic, which asked for exactly that and can be judged in the same sitting. (9) NEW, RULING DD-16: edit a block AFTER loading a DIFFERENT sheet - dump a FET on one schematic, open another, then press Up on that block. The edit is ALLOWED (the three lists are class/flavor settings, not sheet state) and the status line gains ONE EXTRA SENTENCE naming the sheet the dump came from, which appears ONLY when that sheet is not the one on screen. Judge the wording, and judge whether naming the FULL PATH is right or whether it should be the file's tail - the full path is unambiguous but long on a one-line status. (10) NEW, RULING DD-15: this is the one thing a PDK AUTHOR sees, not a schematic user. op_annot::register now REFUSES a declaration whose params carry two rows with the same display label, with a Tcl error at load time naming the type and the repeated label. Judge that error's wording. AND KNOW THE COST: all four shipped PDK register sites are UNCAUGHT, so that refusal ABORTS THE REST OF THAT _procs.tcl file - measured, an unrelated second type never registers. Filed as issue 1328, NOT fixed (the fix touches three PDK files outside this item's scope, for a case no shipped PDK hits). CAVEAT ON (9), FOUND BY THIS ITEM'S ADVERSARY AND FILED AS ISSUE 1329, NOT FIXED: the comparison is a plain STRING compare, so ONE SHEET OPENED THROUGH A SYMLINK is announced as 'not the sheet now open' - a false sentence, never a wrong write (DD-16 rules the cross-sheet edit ALLOWED, so this clause is advice, not a gate). If you judge the wording while standing on a symlinked path, that is what you are seeing.

### the RDW at text size 20 and at 6, and the aA glyph and its tooltip on the user's own X server
every measurement for 1368 was taken on Xvfb :99 with DejaVu Sans Mono; the user's display is the Windows X server over TCP and may substitute another family. Also: does the pane-only scaling read right with an unscaled status line under it, and does the tooltip land where it should at the window's usual position

### the_RDW_cursor_under_an_INACTIVE_selection
R1's ADVERSARY MEASURED THIS IN REAL PIXELS ON :99 AND NO ROW FENCES IT. The cursor shade and Tk's INACTIVE selection colour are 20 of 255 apart. Measured with XGetImage on the mapped pane: cursored line, pane FOCUSED -> selection #4a6984 on shade #d7d7d7, obvious; same line with focus moved to the canvas (which is what the RDW's own focus rule, issue 1308 / DD-12, arranges the moment you click back on the schematic) -> selection #c3c3c3 on shade #d7d7d7. On an UNCURSORED line the same inactive selection is #c3c3c3 on #ffffff, 60 of 255. So a selection you made and then clicked away from is THREE TIMES less distinguishable on the one line the cursor is on. Row CU10 of test_rdw_keys_1245 is the fence for -the selection still shows over the cursor- and its fixture does focus -force .rdw.p.t, so it measures ONLY the focused case; the unfocused one is unmeasured by any row. NOTHING IS BROKEN - the tag order is right (cursor is below sel, verified: raising it above reds CU6 and CU10) and both colours are Tk defaults this item did not choose. THE JUDGEMENT: is a cursored+selected line still readable as SELECTED after you click back to the schematic, or does the cursor shade want to be a step further from #c3c3c3? This lands on item R3 (select and copy, issue 1339) as much as on R1. Also verified in the same pass, and this one is GOOD: the shade really does paint the FULL pane width past the last character - sampled at 2, 25, 50, 75, 90 and 97 per cent of the pane width and at width-3, every one #d7d7d7, against #ffffff on the lines above and below. Row CU7 asserts that in its NAME but measures only the tag range, never a pixel.

### the RDW dump header spelling (spec op_param_lists.md Q6)
you asked for M2B:/xdut/xbg/xamp1; the tree has three spellings and none is that one -- sch_path gives .xdut.xbg.xamp1. (leading AND trailing dot), sim_sch_path strips levels above the raw, and the raw's own is @m.x1.x1.xm2.msky130_fd_pr__nfet_01v8. Default taken: mint yours, with the raw's device path on a dimmer second line

### the_RDW_engineering_notation
SUITES GREEN, PLEASE LOOK - and this item is a NUMBER'S FORMAT, which no count settles. UPDATED IN PLACE 2026-09-05 by item R5 (issue 1341), the attempt that landed; one debt for one formatter, not a second filing. WHAT LANDED: rdw::_value_text now formats a finite value through op_annot::eng_or_blank - THE SHEET'S OWN PROC, not a lookalike - so the window and the schematic cannot print different numbers for the same value, and your ev_precision reaches both (measured: 1.234567e-05 -> 12.35u at the shipped 4, 12.3457u at 6). COUNTS: test_rdw_window_1245 127 -> 134 --nogui and 139 -> 146 on :99 (20 red rows before the fix, ALL PASS after), test_op_param_store_1245 UNMOVED at 130 (its golden BE0 moved with the format), test_rdw_keys_1245 UNMOVED at 59 on :99, test_op_annot control UNMOVED at 485. WHAT TO DO: annotate a sheet (key 6), press 1 over a FET, and put the Results window next to the annotated device. JUDGE: (1) do the numbers in the pane READ THE SAME as the block on the schematic - same digits, same suffix, same count of significant figures - or does one of them still make you convert? (2) THE COLUMN: values now vary in width (11.1u, 1m, 1f, 1.2G) where they used to be a uniform exponent form; the parameter names are still aligned but the VALUES are ragged, because the width is computed from names only. Is that comfortable to read and to paste into a document, or do you want the values aligned too? (3) PASTE A BLOCK into whatever you actually paste into and look at it there - that is the surface this window exists for. (4) A NON-NUMBER passes through untouched (a model name, a `-` placeholder, a geometry already written 1.5u, a two-token value): does a raw string sitting beside engineered numbers read as deliberate? (5) THE ONE DECISION THAT IS NOT NOTATION, and it is on rule debt 1341_nonfinite_in_the_devices_bucket for your ruling: a nan or an inf in the values bucket now says '(did not converge)' instead of printing 'nan'. That word is right, but it is LOSSY - nan, inf and an overflowing 1e400 all read the same. Hard to see on a healthy sheet; if you have a run with a cut-off device, that is the one to look at.

### the_RDW_line_cursor_shade
SUITES GREEN, PLEASE LOOK - and this item is a SHADE OF GREY, which no count can judge. UPDATED IN PLACE 2026-09-05 by item R1 (issue 1337), the attempt that landed; this is one debt for one cursor, not a second filing. COUNTS: test_rdw_window_1245 109 -> 116 headless and 121 -> 128 on the dev display, test_rdw_keys_1245 41 -> 51 on :99, ALL PASS on every arm; test_op_param_store_1245 UNMOVED at 130 and test_op_annot UNMOVED at 485. MEASURED SHADES: light pane #ffffff -> cursor #d7d7d7; dark pane #202020 -> cursor #484848 (an ABSOLUTE +/-40 of 255 that flips to lighter on a dark pane - a 0.88 multiply would give #1c1c1c, four parts in 255, invisible). WHAT TO DO: annotate a sheet (key 6), press 1 over a FET to open the Results window, then click lines in the pane. JUDGE: (1) is the shading NOTICEABLE - your own word - at a glance, and does it read as "the row I am on" rather than as a selection? (2) drag-select across a shaded line: the selection must still be VISIBLE on top of it (the cursor tag is deliberately LOWER than sel, because sel is the lowest tag in the pane and a full-width background above it would hide the selection outright). (3) click the blank space BELOW the text: the cursor stays where you put it and does not jump to the last line - accept, or should a click on nothing clear it? (4) DD-1: press 1 again and the shading goes away completely, because the new dump pushes your row down - you must click again before Up/Down/Delete have a subject. Accept that cost? (5) key 4 (refresh) also clears it when it throws away the block the cursor was in - DD-1 named only the new dump, so that one was the crews choice. (6) the new refusal sentence when no row is marked: "Up: no row is marked in this window - click a parameter row, which shades to show it is the target, then press Up again." (7) THE DARK THEME IS UNREACHABLE IN THIS TREE - ase::palette answers one hard-coded light set, so the dark number above is measured through a stub only. Rule debt 1337 carries (3), (5) and (6) as your ruling.

### the_RDW_raise_behaviour
SUITES GREEN, PLEASE LOOK - and :99 PROVABLY CANNOT REPRODUCE THE DEFECT YOU REPORTED, so this one needs the real screen more than most. UPDATED IN PLACE 2026-09-05 by item R4 (issue 1340), the attempt that landed; one debt for one raise, not a second filing. WHAT LANDED: rdw::push - the single door every dump goes through, keys 1/2/3 and every pick-mode click - now brings the Results Display Window to the front, using the LIBRARY MANAGER'S OWN raise (raise_activate_toplevel split in two: the shared body, issue 0054's wm withdraw + wm deiconify re-map plus issue 0843's deferred recovery, becomes raise_toplevel; the xschem activate_window line MOVED to the other half, not deleted, so the Library Manager, the CIW, create_instance, copy_form, save_as_form, the wave viewer, ASE and alt2_toggle_view - fourteen call sites - keep the activation they are entitled to). COUNTS: test_rdw_keys_1245 :99 53 -> 59 (was 4 FAILED before the fix, ALL PASS on five consecutive runs after), test_rdw_window_1245 124 -> 127 --nogui and 136 -> 139 on :99, test_op_param_store_1245 UNMOVED at 130, test_op_annot control UNMOVED at 485; floors raised 53->59 and 124->127. Also green: test_remap_verify, test_ase_plot 150, test_wave_sigbrowser_i11 74 / i12 126, test_wave_viewer_geometry, and T1 (run_regression.tcl, run solo) at ZERO - 57 cases all 'Total num fail: 0', exit 0. test_ase_window's W7 is 1 FAILED before AND after, measured against the HEAD sources restored into the tree. WHAT TO DO: annotate a sheet (key 6), press 1 over a FET to open the window, then CLICK BACK ON THE SCHEMATIC so the window goes behind it, and press 1 over another device. JUDGE: (1) does the window actually come to the front on YOUR X server? A plain raise scores green on :99 and is an inert no-op on the server you reported from, which is why the suite reads Map/Unmap events and _remap_verify instead of the stacking order - a PROXY, not the thing. (2) DOES IT STEAL THE KEYBOARD? Measured on :99/openbox: the re-map moves focus to the window and it STAYS there unless something catches the grant, so rdw::push now arms the existing one-shot hand-back (rdw::_arm_focus_handback, issue 1306) which gives the canvas back. After a dump, bare 1/2/3/4 and the command mode's Escape must still work WITHOUT clicking the schematic first. If they do not, this is the half you forbade. (3) THE ~32px NORTH-WEST CREEP, issue 0054's known cost: does the window walk up-and-left a little on each dump? The wm geometry restore is in the code and the suite has legs for it, but THOSE LEGS HAVE NO TEETH HERE - measured, a prototype with the restore DELETED scores ALL PASS 59 on :99 because openbox puts the window back by itself. Only your server can answer this. (4) THE COST TO ACCEPT: the pick mode dumps a block per click, so the window now comes forward on EVERY pick. If you have parked it over the schematic it will cover the device you are about to click. There is no is-this-window-occluded question in X, only is-it-above-that-one, so raise-only-when-hidden is not available. (5) DD-6's stated cost: no activation means a strict EWMH window manager may raise the window WITHOUT giving it the active title-bar tint. Is that acceptable, or do you want the tint (which costs the focus)?

### the RDW's five new sentences (item B2d)
SUITES GREEN, PLEASE LOOK. test_rdw_window_1245 grew 32 -> 51 headless and 42 -> 61 on the dev display, ALL PASS on both arms, and 13 sabotage variants each red exactly the row that fences them. A green suite is not an eyeball, and this item put FIVE new strings on screen. HOW TO SEE FOUR OF THEM TODAY: load a raw whose current slot is a dc sweep (or a MULTI-POINT operating point, which save.c renames dc itself), annotate, then Tools > Results Display and dump a FET. The block now carries the DD-5 ANALYSIS SENTENCE between the device path and the incompleteness line - judge whether it reads as a caveat rather than an error, and whether it belongs above the incompleteness line or below it. THE OTHER FOUR need a backend that misbehaves and are unreachable through shipped ngspice; they are on rule debt 1284_four_new_sentences with their exact text, and the one you will meet first is the malformed-answer sentence, because ruling D-5 admits your custom ngspice into this seam. ALSO WORTH A GLANCE: '(no value reported)' where a column has a name but no number, which used to look exactly like an absent column's blank - the two are now different on screen, and the per-block footnote below them is about the blank ONLY.

### the_RDW_select_and_copy_on_VcXsrv
R3: Ctrl-C and drag-extend, on the user's own X server

### the Results Display Window itself (item B3)
FIRST PIXELS of feature 1245. Suites green (32 checks --nogui, 42 on the dev display, 8/8 sabotage variants caught); please look. Judge: the two-line header on screen (bold cadence line M1:/xdut/xbg over the dimmer raw device path) - this is the SAME spelling as the open Q6 look debt, do not rule it twice here; the newest-dump-on-top reading order with older dumps pushed below; the button column Up/Down/Delete/Add/Save on the right, with Add greyed on the annotation list and Delete greyed on 'all'; whether the incompleteness sentence and the five silences read as notices rather than errors in dark orange; and whether the pane is comfortable to select and paste out of at its 96x26 default.

### the merged Waves > Op Annotate entry, on the real screen
it now carries two guards in one expression; the cadence refusal and the no-binding refusal are different sentences and both were only ever driven headless

